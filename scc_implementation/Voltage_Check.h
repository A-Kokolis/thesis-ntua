#define CHECK_STEP 1
#define lineNumber 6

  
double voltage_check(double );  // function to check whether the voltage level has changed or not
void init_voltage_files(void);  //function to store initial voltage levels to appropriate files
